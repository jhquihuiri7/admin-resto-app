import 'package:flutter/material.dart';

class SectionTresPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text('Section Tres')
    );
  }
}
